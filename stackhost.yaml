user:
  name: Japu
  github: Japu-Sahu

language: "javascript"
package_manager: "npm"

python_version: "3.10"

commands:
  build:
    - "npm install -g node@16"  # Install Node.js 16.x globally
    - "node --version"  # Print Node.js version for debugging
    - "pip install -r requirements.txt"
  start:
    - "python3 -m AnonXMusic"

env:
  - "ENVIRONMENT=production"
