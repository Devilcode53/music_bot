user:
  name: Japu
  github: Japu-Sahu

language: "python"
python_version: "3.10"

commands:
  build:
    - "mkdir -p /app/nodejs"
    - "curl -fsSL https://nodejs.org/dist/v16.20.2/node-v16.20.2-linux-x64.tar.xz | tar -xJ -C /app/nodejs --strip-components=1"
    - "/app/nodejs/bin/node --version"
    - "pip install -r requirements.txt"

  start:
    - "/app/nodejs/bin/node --version"
    - "python3 -m AnonXMusic"

env:
  - "ENVIRONMENT=production"
